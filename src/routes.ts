export const publicRoutes = ["/", "/about"];

export const authRoutes = ["/api/auth/signin", "/api/auth/signin/google"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/photos";
